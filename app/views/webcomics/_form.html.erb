<style>
input[type="file"] {height:auto;}
</style>
<%= form_for @webcomic, :html => {:multipart => true}  do |f| %>
  <% if @webcomic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@webcomic.errors.count, "error") %> prohibited this webcomic from being saved:</h2>

      <ul>
      <% @webcomic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :title %> <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description, :rows=>3, :class=>"span6" %>
  </div>
  <div>
    <%= f.file_field :comicimage %>
  </div>
  <div class="actions">
    <br/><%= f.submit "Submit", { :class=>"btn btn-primary" } %>
  </div>
<% end %>
